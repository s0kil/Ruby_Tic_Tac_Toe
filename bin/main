#!/usr/bin/env ruby
require_relative '../lib/tic_tac_toe/interface.rb'
require_relative '../lib/tic_tac_toe/game.rb'

# puts 'Select the row and item index to change'
# puts 'Example: 1.2, first row, second item'
# print ':'
# row_item = gets.chomp

# puts "Do you want #{row_item} to be X or O?"
# row_item_value = gets.chomp

# puts "Changing #{row_item} to #{row_item_value}"

## Show the updated board and ask for the next move.
## We will make a method to check if there is any winner condition in a while loop until player X or O wins.
## A method to display who was the winner.
## Ask the players if they want to play the game again.

interface = TicTacToe::Interface.new
puts interface.new_game
game = TicTacToe::Game.new

player_x = interface.select_option
game.update_board(player_x, 'X')

player_o = interface.select_option
game.update_board(player_o, 'O')

game.board.each { |row| p row }
